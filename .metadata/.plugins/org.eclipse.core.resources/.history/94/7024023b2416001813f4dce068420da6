import java.io.*;
import java.util.*;

public class PashMark {
	public static void main(String[]args) {
		Scanner sc = new Scanner(System.in);
		int x =sc.nextInt(),y=sc.nextInt(),x1=-200,y1=-200,d;
		int in = sc.nextInt();
		
		if(in == x)y1=sc.nextInt();
		else x1= in;
		
		in = sc.nextInt();
		if(in==y);
		else in=y1;
		
		
		
		if(x1!=-200)d=(int) Math.sqrt((x-x1)*(x-x1));
		else d=(int) Math.sqrt((y-y1)*(y-y1));
		

	}
}
